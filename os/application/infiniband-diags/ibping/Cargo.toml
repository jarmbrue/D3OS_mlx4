[package]
edition = "2024"
name = "ibping"
version = "0.1.0"
authors = [ "Sandro Jose Leal Miudo", "Julius Armbruester"]

[lib]
crate-type = ["staticlib"]
test = false
doctest = false
bench = false

[dependencies]
# Local dependencies
runtime = { path = "../../../library/runtime" }
terminal = { path = "../../../library/terminal" }
rdma = { path = "../../../library/rdma"}
